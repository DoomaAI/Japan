# Pasfield family · Japan 2026

A private iPhone Home Screen web app, prepared for a new GitHub repository and Vercel project with Neon Postgres and private Vercel Blob storage. No App Store review is needed.

## Current delivery status

The production frontend builds and the automated model/API checks pass. No GitHub repository, Vercel deployment, Neon database or Blob store has been created or connected yet. Live family sharing and uploads must be checked after those connections are made. The internal browser preview was blocked by the browser environment (`ERR_BLOCKED_BY_CLIENT`), so visual, touch, iPhone installation, calendar import, alarm Shortcut and offline browser checks remain outstanding. Do not treat this as a deployed or fully device-tested release.

## Included

- 16 trip days, 237 editable steps, 6 groups of alternative plans, and all 72 original guide pages.
- Swipe/previous/next navigation, day picker and direct links to days, activities and guide pages.
- Target times in Japan time, original targets, booking times, lock/unlock, completion/start timestamps, skip/reset, notes and participants.
- A rescheduling preview that preserves locked/completed steps and flags booking overlaps. Durations default to editable 30-minute estimates; refine them before relying on rescheduling.
- Parent editing, child completion permissions and per-person private invite links. No email required.
- Neon persistence, optimistic revision checks, an audit history and 15-second foreground refresh. Pending offline progress is kept on the phone and conflicting updates require review.
- Tickets & Reservations: notes, PDF/photo attachments, booking references, luggage tags, search, type/person filters and clickable tags. Associate documents with activities; download critical files explicitly.
- Family gallery: multi-file photo/video uploads, day or activity association, captions, editable tags, day/type/search filters and original-file links. Photos up to 25 MB; videos up to 100 MB. Parent editors upload; the family can view. MP4/MOV/WebM accepted, but playback depends on the phone's codec support. HEIC/HEIF originals are preserved with an open-original link; no conversion or transcoding. Media uses private Blob storage and authenticated byte-range streaming. Uploads require connectivity; gallery videos are not automatically cached offline.
- Ask what food and amenities are near you right now — from the phone's position or from a planned place on the day — with the Japanese name to point at, a rough walk time, walking directions from where you are standing, and one tap to drop it onto today's itinerary or save it to the board. Open to every family member, not just parents. The position is rounded to about a hundred metres and never stored.
- Travel party profiles: each person's age, interests from a fixed list of twenty, what they love, what they would rather avoid, food notes, plus a trip-wide pace and daily budget. Everyone keeps their own; a parent keeps the rest. Used to build suggestions and nothing else.
- AI-suggested ideas for a place and a set of flavours — the famous ones, only-in-Japan, cultural, food, drink, outdoors, with the boys, shopping, after dark — built from the travel party and everything already planned, with a line on why each one suits this family. They arrive without links or hours by design and become ordinary board ideas, tagged as suggested.
- Planning board for the deciding phase, before anything is on a day. Anyone in the family — including the boys — can add a place, meal, event or activity with its location, map link, website, opening/available times, estimated cost, notes, free tags and who it suits. Everyone can vote it up, vote it down or star it as a must-do; the cards carry tags for who added it, who voted each way and whose must-do it is, and the board filters by kind, who it suits, who added or voted on it, and day. A parent can **look a place up on the web** (needs an Anthropic API key) and have its opening hours, ticket page, address, Japanese name, map link, price and typical visit length filled in as an editable draft, with the pages it read listed beside it. A parent puts an idea onto a day with a fixed (locked) or flexible time tag, which creates an ordinary activity carrying the opening hours and cost into its notes, and can then move it between days or lock and unlock its time from the same card. An idea whose activity is deleted returns to the board; one sent to Options says so. Adding ideas and voting work offline; putting one on a day needs a connection. Nothing on the board is on the itinerary, and scheduling an idea does not make a reservation.
- Untimed Options list for ideas, places and missed activities. Move an unlocked step into Options and schedule it later without losing its attachments. Drag handles and accessible up/down buttons reorder the day's steps without changing target or booking times.
- Per-entry HTTPS website/booking links; entries without a saved URL offer a clearly labelled website search. The original first-page cover is used on the welcome and Days screens.
- Embedded family My Map, Maps direction links, bilingual help cards, Google Translate handoff and official app download/website links for Qantas, Disney and USJ.
- Date-specific calendar export with a 15-minute alert. Optional Clock Shortcut handoff for the current Japan day only.
- Offline app shell, locally saved itinerary, opt-in guide/ticket downloads. Plan editing and file uploads require connectivity; progress can queue offline.
- Read-only WebMCP day lookup, when the browser supports it. No supported browser context was available to validate WebMCP registration.

## Setup in GitHub and Vercel

1. Create a **private** GitHub repository, e.g. `japan-family-companion`. Upload this folder's contents to the repository root. It contains your family guide; do not make the repository public.
2. Import the repository into Vercel as a new project. Framework: Vite. Build command: `npm run build`. Output: `dist`. Use Node.js 22 or newer. The included Vercel functions serve all private data; do not deploy only the `dist` directory as a standalone static site.
3. Connect a new Neon database to the project and configure `DATABASE_URL` as a server environment variable. Use the Neon connection string, including its SSL settings. Never prefix it with `VITE_`.
4. Create a **private** Vercel Blob store and connect it. Configure `BLOB_READ_WRITE_TOKEN` server-side (or the Blob SDK's supported connected-store configuration). Do not use a public store for tickets.
5. Set `APP_ORIGIN` to the exact production HTTPS origin, with no trailing slash. Preview deployments should use isolated resources and their own exact origin.
6. Redeploy after setting environment variables.
7. On a trusted development machine, place the production `DATABASE_URL` and `APP_ORIGIN` in the ignored `.env.local` file and run `npm run create-owner` (Node 22+). This creates the database schema and initial itinerary, then writes the private parent link to `.private/owner-link.txt`. It does not print the link, expose a public setup endpoint or overwrite an existing owner. No setup key is used. Deliver the link privately and keep a recovery copy.
8. Open the private parent link, then the avatar → **Our family** → create a link for each family member. Send links yourself. Damien/Lauren links can edit and upload; Nate/Boston links can view and complete assigned activities. Each person opens their link in Safari before adding the app to the Home Screen.

### Getting a new version onto a phone

The service worker takes over as soon as a new build installs (`skipWaiting`), and the app checks for one each time it is brought to the foreground. When a newer build is ready the app shows a **Reload** prompt rather than waiting to be noticed. Without this a Home Screen app can sit on an old build for days, because a waiting service worker only activates once every copy of the app is closed.

### Home Screen icon

The shortcut icon is the guide's own cover — the title block, with Mount Fuji, the pagoda and the cherry blossom, cropped square from `public/cover.jpg` (which is guide page 1). It reads clearly at 60pt on an iPhone Home Screen.

| File | Used for |
| --- | --- |
| `icon-180.png` | `apple-touch-icon` — the iPhone Home Screen shortcut |
| `icon-192.png`, `icon-512.jpg` | Manifest icons, `purpose: any` |
| `icon-maskable-512.jpg` | Manifest `purpose: maskable` — the art is inset so Android's circular crop keeps the whole title |
| `favicon-32.png` | Browser tab |

`any` and `maskable` are deliberately separate files: a maskable icon used as `any` looks needlessly padded, and an `any` icon used as maskable has its edges cropped. The 512s are JPEG because the artwork is photographic — PNG made them roughly nine times larger for no visible gain — and they are left out of the offline shell, since they are only read when installing the app, which always happens online.

**iOS caches the Home Screen icon.** Anyone who already added the app to their Home Screen keeps the old icon until they remove the shortcut and add it again from Safari.

### Environment

| Variable | Purpose |
| --- | --- |
| `DATABASE_URL` | Neon Postgres connection; server only |
| `BLOB_READ_WRITE_TOKEN` | Private Vercel Blob access; server only |
| `APP_ORIGIN` | Exact production HTTPS origin; no trailing slash |
| `ANTHROPIC_API_KEY` | Optional. Switches on the six features that call the Claude API — reading a menu from a photo, reading a document into English, translating a phrase of our own, looking a planning-board idea up on the web, suggesting ideas for a place, and asking what is near here; server only, never prefixed `VITE_` |

Looking a place up and suggesting ideas both run web searches on Anthropic's side, so they take longer than the other calls. `vercel.json` gives the API function a 60-second `maxDuration` for that reason; on a plan whose ceiling is lower, those two are the features that will time out, and everything else is unaffected.

No Google API key is required for the embedded My Map or outbound directions. Google Places/Routes integration and Timeline import are **not implemented in this first build**. App buttons currently use official website/download pages, not undocumented native deep links. Automatic push reminders are also not implemented; Calendar and a user-installed Shortcut are the available reminder methods.

## Family link design

Invites contain 256-bit random tokens in the URL fragment, not query parameters. The browser exchanges the token for a Secure, HttpOnly, SameSite=Strict session cookie and removes the fragment before loading trip data. Neon stores token hashes, not raw invite/session tokens. Role and family identity are checked on the server. Invites/sessions expire after 45 days. Parents can revoke non-owner invites, invalidating their online sessions. The owner recovery link is deliberately protected from in-app revocation; keep it secure.

Offline copies cannot be remotely revoked. **Sign out and clear this phone** removes the local itinerary, progress queue and private downloads on that phone. Only download tickets on family-controlled devices. The API fails closed when production credentials are missing; the local preview mode is explicitly disabled on Vercel and in production.

## Itinerary provenance and review

The attached image-based `Japan Travel Guide.pdf` is the source. Guide pages are served behind authentication. The first-page cover is also a public welcome-screen asset; tickets, travel details and uploaded media remain private. The searchable text was OCR-extracted and can contain recognition errors; the rendered page is the reference. The seeded steps are a working transcription, not confirmed reservations or live timetables.

- **Page 19:** departure/arrival dates conflict with the stated flight duration. Arrival is seeded on 21 September at 20:10 as a flagged item. Check the real Qantas booking before travel.
- **Page 20:** station/line description is inconsistent. The app directs users to live Maps instead of repeating it as verified routing.
- **Pages 40–41:** alternate Kyoto tea/afternoon timings are retained as options. The 27 September weekday typo is corrected to Sunday.
- **Pages 16 and 56–59:** Ueno, Tsukiji and hotel rest are alternative 2 October plans.
- **Pages 16, 46, 47 and 55:** luggage destinations/delivery sequence differ. Confirm with hotels.
- Theme-park passes, height restrictions, opening/show times and transport services have not been independently verified. Booked labels indicate what the guide says, not a check of uploaded confirmations.
- Japanese names are stored for a small set of clearly identified places. Other steps display the English destination and open Maps/Translate to help find the Japanese address. Guide page 3 remains available for additional phrases.

## Offline behaviour

**What a phone can do with no signal at all.** Anything that records what happened, or adds something new, is saved on the phone and handed over when there is signal — it is still right whenever it lands. That covers ticking off activities and missions, skipping a mission, Eye Spy, ride checklists, food ticks and ratings, the phrase of the day, game scores, a diary reflection, adding to the shopping list and marking something bought, acknowledging a family update, Lauren marking her note read, adding a phrase or a dish, saving a written note or link, and putting an idea on the planning board or voting on one. What is left out is deliberate: anything that **reshapes the plan** needs the latest revision to be safe, a stale exchange rate or forecast **overwriting a fresher one** is worse than not saving it, and a janken hand thrown into a queue is not a game.

**One bad update cannot jam the queue.** If the family plan has moved past a queued change — the activity it belongs to was deleted while a phone was out of signal — that one is dropped with an explanation and everything behind it still syncs, instead of sitting at the head of the queue blocking the rest forever.

**Voice notes wait on the phone.** A recording is megabytes of audio, so it cannot go in the little JSON queue; it goes into the phone's own IndexedDB store instead, survives the app being closed, and is uploaded on its own once there is signal. The panel shows what is waiting and lets you delete one unsent. If the signal drops mid-upload the recording is kept rather than lost. Where a phone refuses storage altogether — a private window, storage switched off — the app says so plainly instead of pretending to have saved it.


The build script precaches the app shell and versioned CSS/JS. A signed-in itinerary is saved on the device after successful loading. Guide pages and tickets are downloaded only when the user chooses Save. Downloads are network-first while connected; an authentication failure does not fall back to private cached content. With no connection, explicitly cached pages/tickets remain accessible.

Offline progress stores an operation ID, original revision and timestamp. When reconnecting, a changed server revision prompts review. Repeating a successfully submitted operation does not apply it twice. Other edits require connectivity and are never silently merged. Mobile browsers can evict site data: before travel, check that the app and selected tickets reopen in airplane mode. Keep critical tickets available through their official provider too.

## Phone reminders

Calendar export creates an `.ics` event with absolute UTC times derived from Japan local time, a link to the activity, and a 15-minute display alarm. Test importing on your actual iPhone.

For Clock alarms, install a Shortcut named `Japan Alarm` on each phone: receive text → Get Dictionary from Input → get `time` → Create Alarm; use `label` as the alarm label. Enter that Shortcut name under Remind me. The app enables the Shortcut link only on the activity date when the phone uses Asia/Tokyo. Clock alarms are time-of-day alarms, not dated trip reminders. This path still needs physical-device testing. Editing an itinerary **does not update** previously exported Calendar events or Clock alarms.

## Run locally

```bash
npm ci
npm run dev
npm test
npm run build
```

`npm run dev` provides an explicitly labelled, in-memory parent preview. It does not use Neon or support real file uploads/invites. Restarting the development server resets this preview. Production uses only authenticated Neon-backed data.

## Deployment acceptance checks

Before using the app on the trip:

1. Open the deployed root without a cookie: no itinerary, tickets or guide pages should be accessible.
2. Open the provisioned private parent link and create one parent and one child link. Check the child cannot edit times, add uploads or create invites.
3. On two phones, complete an activity and confirm it appears on the other within 15 seconds. Make competing changes and verify the second is asked to review rather than overwriting the first.
4. Upload a QR image, PDF, photo and short MP4/MOV; assign photos to both a day and an activity. Verify search/tags, video play/seek on both phones, private access and removal. Move an activity to Options and back; verify attachments follow. Verify deleting it retains its files under the day. Test interrupted uploads and retry.
5. Download a guide page and ticket, close/reopen the installed app in airplane mode, record progress, then reconnect. Test the conflict-review path if another phone edits meanwhile.
6. Test swipe navigation, small-screen text, the day picker, guide deep links and the embedded My Map. Map visibility still depends on Google's sharing settings.
7. Test Calendar import and the alarm Shortcut. Verify actual alarm time on the physical phone.
8. Check flight, train, theme-park and luggage details against original confirmations. Adjust duration estimates.

## Updating the app

Deploy source updates through GitHub/Vercel. Existing Neon state is seeded only once (`ON CONFLICT DO NOTHING`); subsequent deployments do not replace edits, document associations, completions or history. Future content corrections should use explicit field-level migrations or the app editor. Never reset the database just to publish an app update. The download-backup button exports itinerary JSON; it does not include ticket file bytes.

## Verification completed in this workspace

- Production Vite build with generated offline precache: passed.
- Automated tests cover seed integrity, time locks, role permissions, alternatives, input validation, rescheduling, Japan timezone, API access/conflicts/idempotency, Options and reordering, attachment associations and upload validation.
- Live Neon/Blob integration and iPhone browser checks: pending account setup/deployment.
- Browser preview: unavailable due to environment block, not a verified visual pass.

Source docs: [Google Maps URLs](https://developers.google.com/maps/documentation/urls/get-started), [Vercel Blob SDK](https://vercel.com/docs/vercel-blob/using-blob-sdk), [Apple Shortcuts URLs](https://support.apple.com/guide/shortcuts/run-a-shortcut-from-a-url-apd624386f42/ios).

## Family companion additions

- **Home / What's next:** current unfinished activity, next locked booking, related tickets and a leave-by estimate. Parents can edit travel minutes and early-arrival buffer in the activity editor. Estimates are manual; there is no live routing calculation.
- **Offline readiness:** select tomorrow or another day, download relevant guide pages and ticket files, and inspect actual cache entries plus the app shell and itinerary snapshot. External booking links are listed for separate checking in their official apps. The readiness screen does not claim external apps, live maps or videos are downloaded. Physical iPhone airplane-mode checks remain required.
- **Running late:** preview shifted unfinished activities and a list to move into Options. Applying is one revision-checked operation. Locked, started and completed steps stay in place; travel buffers reserve room before bookings. Tight fixed bookings produce warnings. Existing phone alarms/calendar exports are not updated.
- **Meeting card:** per-day place/time, English and optional Japanese location details, hotel details, parent contact numbers, large-text and print/PDF views. Contact numbers are intentionally blank until entered. A standard Japanese help phrase is supplied; addresses are not automatically translated.
- **Planning board — the deciding phase:** before a thing has a day, it belongs here. Anyone in the family, the boys included, can put up somewhere to go, something to eat, an event or an activity, with where it is, a map link, a website, its **available times** (opening hours, last entry), roughly **what it costs** in yen with a note like *each* or *for all four*, how long it takes, free tags, and who it suits. Everyone votes it **up**, votes it **down**, or stars it as a **must-do**, and the card wears all of that as tags: who added it, who voted each way, whose must-do it is, who it suits. Filter by kind, by who it suits, by who added or voted on it, by the day you are hoping for, and order the board by most wanted, must-do first, newest or cheapest. A parent then **adds it to a day** and chooses its time tag — *fixed* (locked against rescheduling, the way a booking is) or *flexible*, *optional*, *needs checking* — which creates an ordinary activity carrying the opening hours and cost across into its notes, and assigns it to the people it suits. From the same card a parent can **move it between days**, change its time, and lock or unlock it; the lock is the same lock as everywhere else, so a locked time still has to be unlocked before it moves. The board never lies about where an idea got to: delete the activity and the idea is simply an idea again, send it to Options and the card says *Moved to Options*. An idea nobody wants can be **parked** rather than deleted, and an idea that is on the itinerary cannot be deleted out from under its activity. **Adding an idea and voting on one work with no signal**; putting one on a day reshapes the plan, so that needs a connection. **Nothing on this board is on the itinerary, and adding one to a day does not make a reservation** — the screen says so.
- **Look it up (needs the same API key):** a one-line idea — *teamLab Planets* — comes back filled in. **Look it up and fill in the rest** searches the web through the Claude API and returns the place's **available times** (opening hours, the day it closes, last entry), the **ticket or booking page** as distinct from the official website, the **address and a map link**, the **Japanese name** to hold up to a taxi driver, the price in yen with who it covers, how long a visit takes, what kind of thing it is, whether it needs a fixed time, who in the family it suits, a few tags, and which of the sixteen trip days it fits — chosen from the city they are in that day. Available from the **Add an idea** form and from **Look it up** on any card. It is a **draft in the same editable fields, not an answer you have to accept**: blanks are filled, anything already typed is left alone and offered back under *it also found these* with one **Use these too** button, and nothing is saved until the parent presses save. The pages it read are listed with links, and whatever it is least sure of is printed as the thing to confirm — because hours, prices and whether a ticket is still available change, and a search result can be years old. Everything that comes back is re-checked here before it is shown: lengths are cut to what the board allows, a link that is not HTTPS is dropped rather than shown, a map link that is not actually a Google Maps link is replaced with a plain Maps search for the address, an impossible price or duration is dropped rather than corrected into something that looks researched, a name that is not one of the four is discarded, and a suggested day that is not on this trip is ignored. **It is gathering what is published, not making a booking and not promising anything.** Parent-only, like the menu reader and the translator; it needs connectivity, runs up to eight web searches, and costs a few cents a lookup. When the idea is later put on a day, the ticket page becomes the activity's booking link and the opening hours and cost go into its notes.
- **Who we are, and what we like:** the travel party, on the planning board. Each of the four has their own profile — age, what they are into from a list of twenty (temples, trains, animals, food, drink, onsen, quirky, playgrounds, quiet corners, after dark…), what they love, what they would rather avoid, anything about food, and anything else worth knowing. **Everyone keeps their own**, which is the point: Boston ticking trains and sumo is why the sumo practice comes back, and Nate ticking playgrounds is why the list he gets is different. A parent fills in the ones a five-year-old will not, and sets the trip-wide **pace** (one big thing a day, two or three, or we came a long way) and a rough **daily budget** in yen. The panel shows what the family shares — *Trains & engineering · Nate, Boston* — and says how many profiles are still blank. None of it is on the plan and none of it reaches the alert feed; it is what the suggestions are built from, and what a parent reads when a day needs rescuing.
- **Suggest some ideas (needs the same API key):** pick a place — a day on the trip, anywhere in one of the trip's cities, or somewhere typed by hand — and the flavours you want: **the famous ones**, **only-in-Japan and off the usual list**, cultural, food, drink, outdoors and views, with the boys, shopping and markets, after dark. Claude reads the travel party, the pace, the budget and **everything already on the itinerary and the board**, searches for what is actually on in that city while the family is there, and comes back with four to eight ideas. Each one says what it is, roughly what it costs and how long it takes, who it suits, whether it normally has to be booked — and **one line on why this family**, naming the person or the interest it answers. Put one up with **Put it on the board** and it becomes an ordinary idea the family votes on, tagged *Suggested, put up by …* so it never passes as something we found ourselves, with the reason folded into its notes and *book ahead* added as a tag. **Add and look it up** does both in one tap. Suggestions deliberately **carry no links, no hours and no addresses** — nothing here has been checked, an unchecked address is worse than none, and **Look it up** on the idea is what fills those in. Parent-only, needs connectivity, runs up to five web searches.
- **Food & amenities near here (needs the same API key):** the one asked standing in the street. From **Near here** on the day screen, from the activity card, or from the Help page, it answers what is near enough to walk to right now — somewhere to eat, something quick, coffee, a convenience store, **toilets**, a pharmacy, cash, coin lockers, somewhere to sit down, somewhere for the boys to run, out of the rain. Anchor it on **where I am** (the phone asks first) or on **a planned place** — tonight's hotel or any stop on the day — so it works underground, indoors, or when nobody wants to hand over a position. Each answer gives the name, **the name in Japanese to point at**, what it actually is, a rough walk in minutes, a price band, what it knows about opening times said as the guess it is, and whether a five-year-old will manage it. **Walk me there** opens Google Maps walking directions *from where you are standing* — built by the app from pieces it checked, never from a link the model wrote. Then **Add to today**, which drops it onto the itinerary immediately after whatever you are in the middle of, or **Save to the board** for later. **This one is not a parent's**: whoever needs a toilet is whoever is holding the phone, so every family member can ask, and the boys get *Save to the board* where a parent gets *Add to today*. Tuned for speed rather than depth — four searches, low effort — because an answer that arrives after you have solved it yourself is not an answer. **It cannot see a map, does not know what closed this year, and is not confirming anything is open**; the panel says so under every list.
  **Your position is handled carefully.** It is asked for only when you tap the button, never on opening the app. It is **rounded to about a hundred metres before it leaves the browser** and again on the server — enough to find a convenience store, not enough to point at a hotel room — the rounded figures are shown on screen so you can see exactly what is being sent, and it is **never written into the trip**, so no record of where anyone stood survives the request.
- **Quick capture:** short notes, places, links and optional photos; save to Options or a selected day. Text drafts persist locally. Photos require an online upload and are not retained if the panel closes before upload. If the idea saves but the upload fails, retry attaches to that saved idea.
- **Family updates:** important booking/date/place/plan and meeting changes are visible in-app with who changed them and who acknowledged them. Polling remains every 15 seconds while online; no push alerts are sent.
- **Unified search:** itinerary, Options, reservations/tickets, captions/tags, shopping, challenges, meeting cards, diary and guide OCR. Guide OCR can be searched offline after it has been loaded once.
- **Diary:** completed steps, actual times, challenge discoveries, family reflections and associated media form a daily diary. Export selected day or whole trip as HTML, embedding available JPEG/PNG/WebP photos. Video and HEIC originals remain authenticated links; missing photos are explicitly reported. Exported diaries contain family content and should be shared intentionally.
- **Boys' missions:** Boston is **8**, Nate is **5**. Each has **three missions for every trip day** — 48 each, 96 in all — plus six whole-trip quests, with separate completion timestamps and discovery notes. Every day's set is written around what that day actually holds: Haneda signs on arrival, the Shibuya crossing, teamLab and the sumo, the Nozomi to Kyoto, wait times at Universal, the bamboo grove, the Nara deer, Dotonbori's signs, the Disney queues, Tsukiji's supply chain, Harajuku street style, Beyblade physics, and the lucky cats at Gotokuji. Nate's are observational and hands-on; Boston's ask for evidence, estimates and design reasoning. Parents can add/edit/remove missions, including tasks for both boys. Each child can update only his own challenge progress. Challenge completion and discovery notes can queue offline alongside activity completion.
- **Sound it out:** every Japanese word and phrase the app shows carries an English sound-it-out beneath it — `とんかつ` → **ton-kat-soo**, `ごちそうさまでした` → **go-chee-soh-sa-ma desh-ta**. Hyphenated into even chunks, because Japanese gives every chunk the same length rather than stressing one the way English does, and the screen says so. The devoiced endings a learner reliably gets wrong are written the way they are actually said: です is *dess* not *deh-soo*, ました is *mash-ta*, ます is *mass*. Romaji is kept alongside — it is what you type into a translator. **One chunk is marked in bold where it has to be held for two beats rather than one.** That is deliberately not English stress: leaning on a syllable the way English does is the accent the whole sound-it-out line exists to avoid, and the screen says so. What Japanese has instead is length — a long vowel held twice as long, a double consonant sounded as a beat of silence — and getting it wrong changes the word (おばさん is an aunt, おばあさん is a grandmother). Thirty-nine entries carry a mark because their Japanese has one; the rest are left alone, and a test asserts both halves of that — every mark lands on a real chunk of its own phonics, and nothing is marked that has nothing to hold. This covers all 49 dishes and their 47 versions, the 19 ordering phrases, the 53 phrases in the phrasebook, the 15 menu words, and the fixed phrases on the Show someone card, the dish card and the family meeting card, which now all come from one place (`src/phrases.js`). A parent adding their own dish can give it a sound-it-out too, and a test asserts nothing Japanese in the app is left without one.
- **Hear it, and Hear it slowly:** where the phone has a Japanese voice installed — iPhones do — **Hear it** reads the Japanese aloud at a slightly slowed pace, and the snail button beside it (**Slowly**) reads it at about half speed, chunk by chunk, which is the pace you actually need to copy a phrase. Each speed is its own button, so tapping the other one switches rather than stops. Two things that make a phone say nothing at all are handled: Safari goes silent if `speak()` follows `cancel()` in the same breath, so when something was already talking the engine is given a moment to settle first, and it is resumed in case a previous cancel left it paused. On iOS 16.4 and later the page declares its audio as **playback**. Safari starts every page in the **ambient** category, which is precisely the one the ring/silent switch mutes — so a phone can report that it is speaking and still make no sound. The claim is made **once, when the app starts, and then left alone**: changing the type part-way through a session is known to stop iOS playing anything at all, which is what the app was doing when it asked before every utterance. On an older iPhone, where that API does not exist, a moment of inaudible sound is played inside the first tap instead, which nudges the page off the ambient category. That one is a nudge rather than a guarantee — there is nothing to read back — so it is never reported as having worked. **Headphones bypass the silent switch entirely**, on any iPhone, and the screen says so first because it is the only method that always works.

Two more things silence an iPhone that have nothing to do with the switch, and the app now tells them apart rather than blaming the switch for everything. **Speech often does not work at all inside an app added to the Home Screen** — WebKit's synthesiser is unreliable in standalone mode and fine in Safari itself — so when nothing starts and the app is running from the Home Screen icon, the screen says exactly that and offers to open the same page in Safari. And **the synthesiser wedges once the app has been in the background**, which is every time a phone goes in a pocket; the queue is now cleared each time the app comes back to the front, which is what starts it speaking again without a reload. The message shown is chosen from what the phone actually did — never started in a Home Screen app, never started in the browser, or started and was not heard — instead of one line for all three. The page also declares its audio as playback, and the first utterance — the one Safari reliably ignores — is spent on a silent one so the first phrase anyone taps is the one they actually hear. A **Sound check** panel on the Phrases page turns "I pressed it and nothing happened" into something to act on: it reports the build the phone is running, whether it was opened from the Home Screen or the browser, which voices it found, whether the silent-switch override was accepted, and whether speech actually started, finished, or reported a fault. If speech never starts at all, the phrase says so rather than leaving you tapping — and the page carries the standing reminder that on an iPhone the **side switch silences this too**, which is the usual reason for silence and is not something any app can override. It uses the phone's own speech, so nothing is sent anywhere and it works offline. A phone does not list its voices straight away — Safari returns an empty list and fills it in a moment later — so the app watches for the list to arrive rather than asking once and believing the answer, and treats "nothing listed yet" as unknown rather than as none. The button is hidden only when the phone has actually listed its voices and none is Japanese. When there is a Japanese voice, it is named on the utterance rather than only the language, because left to itself a phone will read Japanese with an English voice.
- **Yen converter:** converts either way, works with no signal, and uses **one rate the whole family shares** rather than each phone guessing. A parent sets it — either by hand or with **Check today's rate**, which looks up the European Central Bank reference rate (free, no key) and *proposes* a value to confirm rather than changing it silently. The rate keeps its cents, because rounding ¥101.37 to ¥101 misreports every conversion. Until someone sets one, the converter says plainly that it is using an estimate and who can fix that. An at-a-glance table covers the prices you actually meet (¥100 to ¥10,000), and the Shopping list now shows a dollar estimate beside every yen budget. The rate lookup is strictly optional: if the service is unreachable the converter still works from the saved rate, which is the case that matters in a shop with no signal. It is a reference rate — a card or ATM will be a little worse once fees and spread are taken out.
- **Food we want to try:** 49 dishes and drinks, each with its English name, the Japanese as it usually appears on a menu, and a pronunciation. Grouped into proper meals, sides, street food, sweet things, drinks and **plain and dependable** — the fallbacks for a hard day with a five-year-old. Tick who has tried what and rate it 1–5 stars each; the card shows the family average, and four or more lands it in **Our favourites**. Rating something also records that you ate it, so the tick and the stars can never disagree. Filter by group, not-tried, tried or favourites. **Show someone** puts the Japanese on screen large enough to hold up at a counter. A parent can add the family's own entries (`Chicken katsu, no sauce`) with their own Japanese, and edit or remove those; the built-in dishes cannot be deleted. Ticks and ratings queue offline. Seventeen ordering phrases — *no wasabi, please*, *is this spicy?*, *do you have a high chair?*, *is there meat in this?* — sit below the list, each copyable in Japanese. Twelve of the dishes that come in versions carry them: tonkatsu as pork loin, pork fillet, chicken or prawn; curry as chicken, pork, beef or vegetable; gyoza as pork, prawn or vegetable; ramen by broth; yakitori by cut. Sushi and maki list what is in them, including the ones with no fish at all — cucumber roll (*kappamaki*), avocado roll, egg, and inari as its own entry — so a child who will not eat raw fish still has something to point at. Each version has its own Japanese and its own **Show**, so you point at the one you actually want rather than the dish in general, and searching *prawn* or *vege* finds the dishes that do it. **The Japanese is a helper for reading and pointing, not a guarantee: menus vary, and anything allergy-related must be confirmed with the restaurant rather than this list.**
- **Read a menu from a photo (needs an API key):** a parent photographs the menu and the app picks out what this family would like, using what everyone has already rated. It returns the dish as printed in Japanese — what you point at — with an English name, who it suits, the price if shown, and one line on why. Dishes already on the food list are matched and labelled; anything new can be kept to the list in one tap. A short *worth knowing before you order* note flags spicy, raw or startling items, and every answer carries at least one thing Nate will eat if the menu has one. The photo is shrunk to 1600px on the phone before it is sent, typically 80%+ smaller, which cuts both cost and time. **This is one of the six places the app calls an external service**, with the document reader, the phrase translator, the planning-board lookup, its suggestions and the near-here panel; nothing else leaves the deployment. It is switched off unless `ANTHROPIC_API_KEY` is set server-side on the deployment; without it the panel says so and the rest of the food page works normally. It needs connectivity, and each read costs roughly one to two cents. **The model can misread a menu and cannot tell you what is in a dish — confirm anything allergy-related with the staff.** The prompt is instructed never to claim a dish is free of an allergen.
- **Read a document (needs the same API key):** on the Tickets page. Photograph a letter, a form, a notice or a receipt, **pick a photo already on the phone**, or choose a PDF — and it comes back **in English**, keeping the document's own order and line breaks so the English can be held against the page in front of you. Above it: what sort of document it is, a short summary led by whatever costs money or has a deadline, and **things to do** with their dates pulled out separately. You can ask a question with it (*"Is there a deadline?"*) and it answers that too. **Copy the English**, or **keep it with our documents**, which files the translation under Tickets tagged `translated` so it is there offline afterwards. A PDF is sent as a document and a photo as an image, which is the difference between an answer and a 400; photos are shrunk to 1600px on the phone first. The prompt is told to copy numbers, dates, times, platform and carriage numbers, codes and prices exactly as printed, never to convert a currency or reformat a date, and **never to guess at a number it cannot see** — if part is unreadable it says which part. **It translates; it does not advise** — no legal, medical or immigration advice, only what the document says and who it tells you to contact. The screen says plainly that a translation can be wrong and a photograph can hide a line, and to check anything with money or a deadline against the original.
- **Theme park ride checklists:** the three park days (Universal 25 September, Disneyland 30 September, DisneySea 1 October) each carry a ride checklist — 15–16 attractions per park with their area, a one-line note, and the minimum rider height. Tick who has ridden what (each person's own tick; a parent can tick for anyone), star the must-dos, and filter by area, must-do or not-yet-ridden. Rides already booked into that day's plan are marked **in our plan**, matched by name so editing the itinerary keeps them in step. Reachable from the park day itself and from More → Theme park rides. Ticks queue offline.
- **Height check:** record each boy's height once and every ride states it plainly — *Boston is tall enough*, *20cm too short for Nate* — instead of leaving a number to be compared in a queue. **The heights and ride names are a planning aid gathered before the trip, not a confirmed source.** Parks change requirements and rides close; the panel says so and links to the official app. Confirm anything you are planning around, especially for Nate.
- **Park maps:** each park panel links to the official app (live map, wait times, Premier Access/Express Pass) and the park website. The parks' maps are their copyright, so the app links to them rather than shipping a copy. To hold one offline, save a screenshot or the app's PDF under Tickets, tagged `park map` with the park's name in the title; it then downloads with the rest of that day and the panel links straight to it. Every ride also has a **Maps** link that searches Google Maps for that attraction. Note that Google does not route usefully *inside* a park — use it to place things before the day, and the park's own app for live position and wait times. To put ride pins on the family My Map, add them in Google My Maps yourself; the app cannot edit your map.
- **Phone numbers, calling and WhatsApp:** an activity can carry a phone number, shown on its card with **Call** and **WhatsApp** buttons. Call uses a `tel:` link, so the phone dials it. WhatsApp uses an official `wa.me` click-to-chat link, which opens a chat with that number — the other end must have WhatsApp, and this does not place a WhatsApp voice call. A number written the Japanese way (`075-241-0111`) has its leading `0` replaced by `+81`, because that is how a Japanese number is dialled internationally; the app states the number it will dial (`Dialling +81752410111`) both in the editor and on the card, so an Australian mobile written as `04…` is visible as wrongly assumed rather than silently misdialled. Start with `+` and a country code for anything outside Japan. A number too short to be international still gets a Call button but no WhatsApp link. Numbers also appear on Options entries.
- **Window I spy on the Shinkansen:** the two Nozomi legs (Tokyo → Kyoto on 24 September, Kyoto → Tokyo on 29 September) show a **Window I spy** panel with 16 things to find from the train — Mount Fuji, a Shinkansen passing the other way, tunnels, rice fields, golf-range nets, the snack trolley, the cleaning team bowing on the platform. Each boy keeps his own list on each leg, so the two can play the same leg separately and the return trip starts fresh. Mount Fuji's hint follows the direction of travel: on the **right** about 40 minutes out of Tokyo heading west, on the **left** about 40 minutes before reaching Tokyo coming back. Ticks queue offline, which matters through the tunnels. Each boy ticks only his own list; a parent can tick for either.
- **Skipping and new missions:** a boy can skip a mission he does not want with **Not this one**, and it leaves his list without counting against his progress. Skipped missions collect under a **Skipped** section and can be brought back at any time. Skipping clears any tick on that mission. **Give me a different mission** draws a fresh one from a reserve of ten per boy — these are written to work on any day, so they suit whatever is happening. Up to three extra missions per boy per day; each draw is a mission he does not already have that day, and drawing is refused once the limit is reached. Each boy can only skip and draw for himself; a parent can do either for either boy. Skips queue offline alongside completions; drawing a new mission needs connectivity. Nothing is generated by an AI — the reserve missions are written in advance, so this works offline and costs nothing to run.
- **Pictures on missions:** every mission carries an emoji cue, shown large on Nate's cards and smaller on Boston's. Seven of Nate's missions, where the shape is the point, also carry a simple line diagram — the torii gate, the Shibuya crossing, bamboo rings, the bridge arches, a scoreboard, a spinning top and the lucky cat's raised paw. Diagrams are inline SVG, so there are no image files to download and they work offline. A parent adding a mission can give it an emoji of their own.
- **Missions open first for the boys, and read themselves aloud:** Nate and Boston land on the Missions screen when they open the app, rather than the day plan — a link that names a screen (`?tab=`, `?step=`, `?page=`) still wins. Every mission card has a **Read to me** button so Nate can follow his own missions before he can read them. It uses the phone's own speech, so nothing is sent anywhere and it works offline; one mission reads at a time, and tapping the same button stops it. The button is hidden on browsers without speech support, and the voice is whichever en-AU voice the phone has installed.
- **The morning reminder:** on the day screen, first thing, a line about what to take — *Umbrellas today. Jumpers — it is cool, especially first thing and after dark.* It is about the jumper and the umbrella rather than the meteorology, appears only while it is still morning in Japan and only on the day you are actually on, says nothing at all on a pleasant day, and goes away for the day once someone has read it. It reads the forecast already on the phone, so it works with no signal. **It cannot ring a phone that is not open** — the app sends no push notifications, so this is a reminder you meet when you open it, not one that comes and finds you.
- **Weather:** the forecast for the day you are looking at, on the day screen — a temperature, what the sky is doing, the chance of rain, and the four days after it. It comes from [Open-Meteo](https://open-meteo.com), which is free and needs no key or account, and it is asked **once per place the trip visits** rather than once per day, so a week in Kyoto is one lookup. The Disney days are asked about as Urayasu on the bay, which is not central Tokyo. The answer is kept **in the trip**, so one phone's lookup serves the whole family and the numbers are still on screen with no signal at all — the card says how long ago it was checked and by whom, because a stale forecast is worse than none. It also says what to do about it: umbrellas, water bottles, layers for Nate, or an indoor option when there are storms about. Readings that cannot be true — a minimum above the maximum, a temperature off the scale, a missing code — are dropped rather than displayed, and the same checks run again on the server before anything is stored.
- **Games for the boys:** ten, under More → The plan → **Games**. All but one work with no signal at all.
  - **Match the letters** — a six-pair memory game over hiragana or katakana, tap the letter then the sound it makes. Where the phone has a Japanese voice it reads each letter aloud at half speed as it is turned over, so Nate is matching a shape to a sound rather than to a squiggle. Best score kept per person per alphabet, fewer taps being better.
  - **Read the sign** — the katakana decoder, and the one that pays off on a real menu. Every foreign word in Japanese is written in katakana, so a child who can sound out コーヒー can read half a café menu. Forty real words off menus, signs, vending machines and lift panels, each with a line on where it turns up. A run counts up while the answers are right and resets when one is wrong.
  - **Janken** — rock, paper and scissors as Japan actually plays it (グー・チョキ・パー, called with *saisho wa guu, jan-ken-pon*), across two phones. **Neither hand is sent to the other phone until both are thrown**: the redaction happens at the response boundary in `server/visibility.mjs`, not in the screen, because a hidden screen is one fetch away from being read. A thrown hand cannot be changed, a draw scores nobody, and the running tally is shared. It needs both phones online and polls a little faster while a hand is waiting to be answered.
  - **Onigiri to Fuji** — the merge game, on a four by four board. Two of the same become the next one up, climbing a Japanese ladder: rice ball, dango, ramen, sushi, maple leaf, cherry blossom, torii gate, Nara deer, castle, Shinkansen, Mount Fuji. Swipe or use the arrows. A pair merges once per move, not in a chain, and the game ends when there is no space and no pair left anywhere.
  - **What we did** — a memory game built out of the trip itself: match the thing we did to the day we did it. It only appears once a few activities have been ticked off, and it grows as the trip does, so by the last week it is a quiz about your own fortnight.
  - **Japan pairs** — picture memory over eighteen things you will actually see: a torii gate, a Shinkansen, a lucky cat, a hot spring. Each card gives the English and the Japanese, so it teaches the names while it is being played. Emoji rather than image files, so it adds nothing to download and works with no signal.
  - **Make it** — two things make a third. Start with rice, water, fire, fish, soy beans, wheat, tea leaves and seaweed, and work out the other sixteen: rice and water make cooked rice, cooked rice and seaweed make an onigiri, noodles and miso soup make ramen. Every one is something a child could be told and would believe, everything is reachable from the eight you start with, and no recipe makes one of its own ingredients.
  - **Sushi snake** — snake, eating sushi. It starts at the near wall rather than the middle and moves at a pace a five-year-old can steer, which is about ten cells of room before the far wall instead of the second and a bit a mid-board start gives you.
  - **Sumo stable** — the merge-and-battle one. Sixteen squares; tap two wrestlers of the same rank and one of them is promoted, up the **real sumo ladder** — jonokuchi, jonidan, sandanme, makushita, jūryō, maegashira, komusubi, sekiwake, ōzeki, yokozuna — so it teaches the ranks on the way up. Build one big enough and send him out against a challenger near your own rank. A bout is decided mostly by rank but never entirely: a yokozuna is not certain and a beginner is not hopeless, because an upset is the only reason to watch. Winning pays by the rank you beat; losing costs your best wrestler a rank. Two of the same rank only, nothing goes above a yokozuna, and a full stable takes no more recruits.
  - **Sumo** — tap faster than the other one and push him out of the ring, calling it properly (*nokotta, nokotta!*). Three speeds — Nate-speed, Boston-speed and Dad-speed — and a win against a faster opponent is worth more, so the best score says who you beat.
  Scores go up and never down, each person keeps their own, and the boys can only score for themselves. **A score earned with no signal is kept and synced later**, like a completed mission — the phone shows it straight away rather than looking like it did nothing. A janken hand is deliberately not queued: a hand thrown into a queue is not a game.
- **Shopping:** shared list with item, quantity, intended person, optional day, shop, HTTPS link, notes and total yen budget per item. Anyone in the family can add and mark bought/unbought; parent editors can change details or remove entries. Shopping edits currently require connectivity.
- **Voice notes:** anyone in the family can record a voice note — during something, at the end of it, or at the end of the day — and everyone plays it back on their own phone. Reachable from the day (**Voice**), from an activity card (**Voice note**), from More → **Voice notes**, and from the Diary, where each day's notes sit with that day's photos. A note is attached to a day, or to one activity on that day; the activity list only offers that day's stops, not all 237. Up to five minutes each, and it stops itself at five. The boys record their own and can delete their own; a parent can remove any of them. It uses the phone's own microphone through the browser, so on an iPhone use Safari and allow the microphone when it asks. **A recording needs signal to save, and is held on the phone only while the panel is open** — the screen says so rather than pretending otherwise. Storage enforces what a file is: the server reads the type and size back from private Blob storage rather than believing the phone, and a note can only be written into the recorder's own folder. Requires `BLOB_READ_WRITE_TOKEN`, the same private storage the tickets and photos use.
- **A phrase a day, and a phrasebook:** one new Japanese phrase pops up for each person on each day of the trip — *konnichiwa* on day one, working through *sumimasen*, *ikura desu ka*, *nan-banshen desu ka* and ending on *sayōnara*. Sixteen days, sixteen phrases, none repeated. Each person marks their own **Got it**, so the boys and the parents are not tied to each other's pace, and it is recorded per person per day rather than per phone. **Swipe for more:** the pop-up is not limited to the one phrase — swipe left (or tap **One more**) to keep going through the book, swipe back to re-read, and it never offers a phrase that person has already met. Arrow keys work too. It waits for a clear screen: on Lauren's phone her private note comes first and the phrase follows once she has closed it, so the two never stack up. Dismissing it works offline (it is remembered on the phone and synced when there is signal). The full **Phrases** page, under More → Out and about, holds all 53 — greetings and manners, when you are stuck, finding our way, trains, shops and paying, with the boys, and if something goes wrong — plus 12 words to recognise on a menu or a sign. Search matches English, Japanese, romaji or the sound-it-out, and ignores macrons and punctuation, because nobody types *arigatō* on an English keyboard. **Phrases you have seen** sits at the top of that page: a running count (7 of 53), a **Show me another** button that hands over the next one nobody has shown you yet, and **My log** — every phrase you have actually been put in front of, newest first, with the day you met it. The log is per person, so each of the four has their own; only what was really on screen goes in it, and the first time you met a phrase is what is kept. Everything is practical and polite enough to use with a stranger; nothing is generated, so it works offline and costs nothing to run.
- **Our own phrases (needs the same API key):** a phrase you need that the book does not have — type it in English, tap **Put it into Japanese**, and it comes back with the Japanese, the romaji, the sound-it-out and, importantly, **what the Japanese literally says back in English**, so you can see what you are about to ask for. It is a draft in editable fields, not an answer you have to accept: change anything, then **Keep this phrase**. It then sits in *Our own phrases* on the Phrases page with a **Hear it** and a **Slowly** button like everything else, and the page search covers it. **Translation can be wrong, or right but strange to say** — the screen says so, and every field stays editable afterwards. A parent can also skip the translation entirely and type the Japanese in by hand, which works with no key and no signal. The server checks what it stores rather than trusting what came back: English and Japanese both required, the Japanese actually has to contain Japanese characters, lengths are bounded, and a phrase that was typed is recorded as typed rather than claiming to be translated. Parents add, edit and remove; the boys read. These stay out of the daily rota and the phrase log, which are built from the book's own fixed ids.
- **Daily notes for Lauren:** a private thank-you note from Damien that pops up for Lauren once per trip day. See the section below.

Navigation is driven by one registry (`src/nav-data.js`) that feeds both the bottom bar and the More screen, so every page is reachable from exactly one place and a new page cannot be orphaned — a test asserts it for each family member.

The bottom bar holds five destinations plus More, chosen by who is holding the phone. Parents get **Home · Days · Tickets · Food · Yen**; Nate and Boston get **Home · Days · Missions · Food · Diary**. Everything else sits under More, grouped into *Out and about*, *The plan*, *Looking back* and, on Damien's phone only, *Just for you* — as compact rows with icons rather than a flat wall of cards. The More button carries a dot when there are unread family updates, and it highlights whenever you are on any page it holds, so you never lose your place on a sub-page. New main pages have `?tab=...&day=...` URLs, with item links for search destinations.

New state fields are initialized only when absent, preserving existing family edits and challenge completions. No database reset is required. The fuller mission sets are merged into an existing trip once, keyed by `missionSeed`: every completion, discovery note and parent-written challenge is kept, and a superseded single mission is dropped only when nobody completed it. The provided final iframe uses map ID `1mztIuWzTviCEZSLdDxEUqo2WK3HUNfo`; the separate viewer button uses that same ID. The temporary different map ID was replaced with this final supplied embed. Map visibility could not be verified in this environment; Google sharing settings still govern access. [Google's embed and sharing instructions](https://support.google.com/mymaps/answer/3109452?hl=en).

Additional validation: 18 automated test groups passed, including age-correct mission assignment, child permissions, shopping, acknowledgements, delay/backlog handling, offline manifests, unified search and diary dates. All 10 added screen components also rendered successfully in a server-render smoke check. This is not a browser visual or physical iPhone pass. Test actual uploads, map display, offline storage and video playback after deployment.


## Daily thank-you notes (Damien → Lauren)

A private note from Damien appears as a pop-up for Lauren once on each day of the trip.

**What each person sees**

| | Pop-up | The note list | Schedule and read receipts |
| --- | --- | --- | --- |
| Damien | No | Yes — writes, amends, reorders, pins, removes | Yes |
| Lauren | Yes, once per trip day | No | No |
| Nate, Boston | No | No | No |

**How the schedule works.** The notes are a single ordered list, seeded with 20 written suggestions. Each trip day takes the next note in list order, so reordering the list changes which day gets which note. A note can instead be pinned to a specific day; pinned notes always land on their day and the unpinned ones fill the days around them. A day accepts only one pinned note. With 20 notes and 16 trip days, the surplus sits at the bottom of the list as spares until Damien moves one up. Damien can amend any wording, add his own notes, or delete ones he does not want.

**Read receipts.** Damien's page shows whether Lauren has opened each day's pop-up. A tally above the schedule counts the notes opened out of those already delivered and names any she has not opened. Each schedule row and each note card then carries its own state: a green tick and the Japan time she opened it, or `Not opened yet` for today, `Not opened` for a day that has passed, and `Waiting` for a day still to come. A note opened after midnight in Japan shows the date she actually read it (`Read Wed, 23 Sept · 07:05 JST`), so a late read is not mistaken for one on the day. Only days that have arrived and carry a note count towards the tally. Lauren sees her own receipt on the note itself; the tally and the schedule are Damien's alone.

**How Lauren sees it.** The note opens automatically the first time she opens the app on that day, in Japan time. Closing it records that she opened it and it does not reopen. A heart in the top bar reopens the day's note whenever she wants it, with a dot until she has read it. Dismissal is also recorded on the phone itself, so the note does not reappear if she is offline when she closes it.

**Privacy.** This is enforced on the server, not only in the interface. `server/visibility.mjs` redacts the trip state at the response boundary: Damien receives the full list, Lauren receives only the text scheduled for the current Japan day, and Nate and Boston receive neither. The note text is never sent to a phone that is not meant to read it, so it is not in the cached offline snapshot or the itinerary backup download for those family members. The notes are also kept out of Family updates and the "Recent family changes" history, so no one sees that a note was written or read. Damien is the only person who can write or change a note; Lauren is the only person who can mark one read. Both rules are checked server-side by family-member name, not only by parent role.

**Limitations.** The pop-up appears when the app is opened or refreshed — it is not a phone push notification, and nothing is sent if Lauren does not open the app that day. Marking a note read needs connectivity; offline, the phone remembers the dismissal and the read receipt is simply not recorded. Anyone holding Damien's parent invite link can read and edit the list, so treat that link as private.

## Imported map locations and guide directions

The supplied `Japan_2026_Google_Maps_Master_List_SELECTED_ADDITIONS.xlsx` contains 209 location rows in Master List. All are retained in `data/map-locations.json`, with original venue, district, city, type, address, trip notes, source URL and row number. The workbook itself was not modified. The import script is a read-only OOXML extractor, with a reviewed exact-alias table for itinerary wording.

- 72 distinct imported venues match 186 existing activity steps. Matching directions now use venue plus supplied address. Broad areas, unmatched venues and special station entrances keep their original destination text. Explicit location selection is available in the activity editor; changing the free-text place detaches the old link.
- The app's original guide-page viewer has a direction-links panel for relevant locations. This adds navigation beside the original guide images; it does not change or reissue the original PDF.
- Places includes all 209 rows with city/district/type filters and text search, walking/transit/driving links, source websites, guide-page links and the ability to save a location to Options or a day. Unified search includes imported addresses and notes.
- Separate branches and source duplicates are retained. Explicit itinerary aliases resolve the intended listing where reviewed, including TOTARO; broad ambiguous matches are not guessed. The source's reference-only / closed-location warning for Jishu Jinja is preserved. Source confirmation/opening claims have not been independently reverified.
- Locations are served through authenticated trip state, not bundled in the public client. Loaded addresses are included in the locally saved itinerary; live directions still require Google Maps connectivity or its own offline preparation.
- These links use Google Maps universal URLs, requiring no Google API key. No Place IDs were supplied; destination pins and live route/entrance choices should be checked in Maps. [Google Maps URL documentation](https://developers.google.com/maps/documentation/urls/get-started).

Validation now includes 20 automated test groups: all 209 rows retained; URL encoding and mode validated for 627 direction links; exact branch matching, custom destination detachment, guide-page associations and invalid location rejection covered. Both new location components rendered direction links in a server-render smoke check. Live iPhone navigation remains a deployment check.


### Turning the guide

All 72 pages turn like a book: **swipe the page sideways**, or use the arrow keys, as well as the buttons. Every way of turning goes through one place, so a swipe, a key and a button cannot drift apart, and it **stops at both covers** rather than wrapping — page 1 arriving after page 72 is disorienting when you are hunting for something. A swipe has to be sideways rather than a scroll (55px across, under 45px down), the page still scrolls up and down normally, an arrow key typed into the page box or the day list is not a page turn, and the keys are only listened for while the guide is open.

### Day guide previews and ticket attachments
Each day now shows a swipeable strip of its original guide pages above the activities. The guide reader includes a day selector and return-to-day button.

Tickets & reservations remains the shared central document area. Save a ticket first, then expand Add photos / files to this ticket to select multiple images/PDFs and label each person. Individual attachments support opening, offline saving, person/tag edits and removal. Removing the parent ticket removes all its attachments from the shared itinerary (already downloaded copies remain). Upload retries retain successful registrations. Existing single-file tickets remain supported. Production Blob uploads still require a live deployment check.

Every row in Tickets & reservations leads with a tappable thumbnail. A ticket that is a photo shows itself; a written luggage tag or a PDF shows the first photo attached to it, with a badge counting the files; a PDF with no photo shows a labelled PDF tile. Tapping any of them opens the full-screen viewer at that picture, so you can swipe the rest of the set from there. Attached files carry the same thumbnail, so a PDF attachment is now a tile rather than a blank line. HEIC/HEIF photos and videos cannot be drawn by most browsers, so instead of a broken image they show a labelled tile that opens the original — which an iPhone renders natively. Written notes show a quiet, non-tappable tile so every row keeps the same shape.

Opening any ticket file full-screen reads the whole set: the ticket's own file first, then each file attached to it. Swipe left and right, use the Previous/Next buttons, or press the arrow keys to move between them; Escape closes. A counter shows the position, and navigation is hidden for a ticket holding a single file. Written details and external links carry no file, so they are skipped. Swipe works on photos; a PDF is shown in an embedded viewer that takes its own touches, so use the buttons or arrow keys there.

Saving a ticket now clears the add form, so the next one starts blank. Previously the title, reference, notes, tags and link stayed filled in and the chosen file was still attached to the file input, which risked saving a duplicate on the next entry.


### Guide-inspired visual theme
White paper surfaces, fine rules, black Playfair Display headings and Roboto Condensed labels closely follow the original guide images. Font files and OFL licences are bundled under src/fonts; Vite emits them as versioned assets included in the offline shell. Exact source typefaces could not be identified from the raster guide. Theme styles live in src/guide-theme.css. Original guide pages and cover remain unchanged. Production build checked; live iPhone visual review remains pending.
