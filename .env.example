DATABASE_URL=
BLOB_READ_WRITE_TOKEN=
# Generate a random 32-byte hex secret locally and paste into Vercel environment settings.
SETUP_SECRET=
# Exact deployed HTTPS origin, no trailing slash.
APP_ORIGIN=
